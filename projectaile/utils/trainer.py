class TRAINER:
    def __init__(self, config, model, feeder, callbacks):
		self.config = config
		self.model = model
		self.feeder = feeder
		self.callbacks = callbacks
		
	def train(self):
		# Run Training And Validation Epochs And Callbacks
		return
		
	def train_step(self):
		return